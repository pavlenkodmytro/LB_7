#include <iostream> // Підключаємо бібліотеку для вводу-виводу
using namespace std; // Використовуємо простір імен std для спрощення коду

int main(void) { // Головна функція програми


    double pi4 = 0.; // Ініціалізуємо змінну для обчислення частини числа π/4
    long n; // Оголошуємо змінну для зберігання кількості ітерацій

    
    cout << "Number of iterations? "; // Запитуємо користувача про кількість ітерацій
    cin >> n; // Зчитуємо введене число в змінну n


    
    for (long i = 0; i < n; i++) { // Цикл для n ітерацій
        // Додаємо або віднімаємо 1/(2*i + 1) залежно від парності i
        pi4 += (i % 2 == 0) ? 1. / (2. * i + 1.) : -1. / (2. * i + 1.); 

        
        /*
pi4 += ...:
            Це означає, що до значення pi4 буде додано (або від subtract) новий термін. 
            Знак += означає, що ми беремо значення pi4 і додаємо до нього результат виразу праворуч.
(i % 2 == 0):
            Цей вираз перевіряє, чи є i парним числом. Оператор % – це оператор залишку від ділення. 
            Якщо i парне, то залишок від ділення на 2 буде 0.
? ... : ...:
            Це тернарний оператор, який є короткою формою умовного оператора if. Він працює так:
            Якщо умова перед знаком питання ((i % 2 == 0)) є істинною, то виконуються дії після знаку питання.
            Якщо умова є хибною, то виконуються дії після двокрапки.
1. / (2. * i + 1.):
            Це вираз, який обчислює додатній термін, якщо i парне. 1. і 2. – це дійсні числа (double), щоб забезпечити точність при обчисленнях. 
            У результаті, при парному i, ми отримуємо непарне число у знаменнику (1, 5, 9, ...).
-1. / (2. * i + 1.):
            Це вираз, який обчислює від’ємний термін, якщо i непарне. 
            Як і в попередньому випадку, ми знову маємо дійсні числа.
        
Точка після 1 і 2 означає, що ці числа є дійсними (типу double). В C++:
            1 і 2 – це цілі числа (типу int).
            1. і 2. – це дійсні числа (типу double).

    -----------------------------------------------------------------------------------------------------------------------
        
         i % 2 == 0 перевіряє, чи парне число i. Якщо так, то до pi4 додається 1/(2*i + 1), якщо ні, то від pi4 віднімається 1/(2*i + 1).
         Це реалізує чергування додавання і віднімання, що є характерним для формули Лейбніца.

        Чому саме (2. * i + 1.)?
        
        2*i+1 генерує непарні числа (1, 3, 5, 7, ...). Це важливо, адже терміни формули Лейбніца використовують саме непарні числа.
        Кожен новий термін зменшує внесок до загальної суми, що забезпечує поступове наближення до π.

-------------------------------------------------------------------------------------------------------------------------------------
        Чому від парності?
        Парні індекси (i = 0, 2, 4...): Коли i парне, ми додаємо значення, тобто 1/(2*i + 1). Це відповідає доданню терміну до суми.
        Непарні індекси (i = 1, 3, 5...): Коли i непарне, ми від subtract значення, тобто -1/(2*i + 1). Це відповідає віднімання терміну з суми.


        Коли i парне (0, 2, 4, ...): ми додаємо 1/(2i+1). Це відповідає доданню терміну, який має непарне значення (1, 5, 9, ...).

        Коли i непарне (1, 3, 5, ...): ми віднімаємо 1/(2i+1). Це також відповідає непарним значенням (3, 7, 11, ...).

        Таким чином, чергування додавання і віднімання дозволяє формулі точно наближатися до 𝜋/4, а отже, і до 𝜋.

----------------------------------------------------------------------------------------------------------------------------
        ЩО ТАКЕ ТЕРМIНИ?
        https://drive.google.com/file/d/1llAj2tqH_EBSBx74KS04xtgtRpLC757O/view?usp=sharing

*/
        
    }

    cout.precision(20); // Встановлюємо точність виводу на 20 знаків після крапки
    cout << "Pi = " << (pi4 * 4.) << endl; // Обчислюємо та виводимо значення π, помноживши pi4 на 4

    //Значення pi4, яке ми обчислили, множимо на 4, щоб отримати наближення до числа π, і виводимо результат.

    return 0; // Завершуємо програму
}
